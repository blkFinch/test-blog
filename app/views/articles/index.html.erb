<!DOCTYPE html>
<html>
<head>
  <title>index</title>
  <style type="text/css">
    
  </style>
</head>
<body>
  <h1>Listing Articles</h1>
  <table>
    <tr>
      <th>Title</th>
      <th>Text</th>
    </tr>

    <% @articles.each do |article| %>
    <tr>
      <td><%= article.title %></td>
      <td style="width: 150px;"><%= article.text %></td>
      <td><%= link_to 'Show', article_path(article) %></td>
      <td><%= link_to 'Edit', edit_article_path(article) %></td>
      <td><%= link_to 'Delete', article_path(article),
            method: :delete,
            data: {confirm: 'Are you sure?' } %></td>
    </tr>
    <% end %>
  </table>
  <p style=" margin: 10px;"><%= link_to 'Submit New', new_article_path %>

</body>
</html>